mixin icon(data)
  svg(
    role='img'
    class=['icon', 'icon_'+data.icon, data.className]
    style='width:' + data.width + '; height:' + data.height
  )
    use(xlink:href='svg-symbols.svg#'+data.icon)
